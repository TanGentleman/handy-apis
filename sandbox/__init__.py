"""Sandbox utilities for Modal-based agent environments.

Modules:
    image    - Image construction (build_agent_image, add_local_repo, etc.)
    sandbox  - Generic sandbox creation (create_sandbox, run_prompt)
    docs     - Documentation upload to Modal Volumes (upload_collections)
    opencode - OpenCode serve mode + CLI wrapper
"""
